import { ReactElement } from 'react';
import '@/components/Header.scss';

function Header(): ReactElement {
  return (
    <div className="Header">
      <h1>Vite + React Example App</h1>
    </div>
  );
}

export default Header;
